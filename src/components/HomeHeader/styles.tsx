import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flexDirection: "row",
        alignItems: "flex-start",
        height: "auto"
    },
    title: {
        flex: 1,
        color: "#FFFFFF",
        fontSize: 25,
        fontWeight: "bold"
    }
})
