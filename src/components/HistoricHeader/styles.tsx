import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flexDirection: "row",
        justifyContent: "flex-start",
        alignItems: "center",
        height: "auto",
        gap: 8
    },
    title: {
        flex: 1,
        color: "#FFFFFF",
        fontSize: 25,
        fontWeight: "bold"
    },
    clear: {
        flexDirection: "row"
    }
})
